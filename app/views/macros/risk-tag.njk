{% from "govuk/components/tag/macro.njk" import govukTag %}

{#
Risk is a six-value enum (None, Very low, Low, Medium, High, Pending) per the report
schemas. Colours match MSH's riskClasses mapping (src/utils/Tags.tsx) exactly.
"Pending" and null both mean not yet assessed, so both get the blue "Pending analysis"
tag.
#}
{% macro riskTag(risk) %}
  {% if risk == "High" or risk == "Critical" %}
    {{ govukTag({ text: risk, classes: "govuk-tag--red" }) }}
  {% elif risk == "Medium" %}
    {{ govukTag({ text: risk, classes: "govuk-tag--yellow" }) }}
  {% elif risk == "Low" %}
    {{ govukTag({ text: risk, classes: "govuk-tag--green" }) }}
  {% elif risk == "Pending" or not risk %}
    {{ govukTag({ text: "Pending analysis", classes: "govuk-tag--blue" }) }}
  {% else %}
    {{ govukTag({ text: risk, classes: "govuk-tag--grey" }) }}
  {% endif %}
{% endmacro %}
