{#
Renders the object from app/lib/charts/bar-chart.js as an SVG bar chart. Each bar has a
native <title> tooltip plus a visible label underneath, so colour and height are never
the only way to read a value.
#}
{% macro barChart(title, chart) %}
  <div class="msh-bar-chart">
    <svg viewBox="0 0 {{ chart.width }} {{ chart.height }}" preserveAspectRatio="none" class="msh-bar-chart__svg" role="img" aria-label="{{ title }} — see the labels below for exact values">
      {% for bar in chart.bars %}
        <rect x="{{ bar.x }}" y="{{ bar.y }}" width="{{ bar.width }}" height="{{ bar.height }}" class="msh-bar-chart__bar">
          <title>{{ bar.label }}: {{ bar.value }}{% if bar.alertCount is defined %} ({{ bar.alertCount }} alert{{ "s" if bar.alertCount != 1 }}){% endif %}</title>
        </rect>
      {% endfor %}
    </svg>
    <div class="msh-bar-chart__labels">
      {% for bar in chart.bars %}
        <div class="msh-bar-chart__label-item">
          <span class="msh-bar-chart__label-month">{{ bar.label }}</span>
          <span class="msh-bar-chart__label-value">{{ bar.value }}</span>
          {% if bar.alertCount is defined %}
            <span class="msh-bar-chart__label-sub">{{ bar.alertCount }} alert{{ "s" if bar.alertCount != 1 }}</span>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>
{% endmacro %}
